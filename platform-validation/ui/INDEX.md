# Optional platform UI

Minimal application-owned Request interface, packaged only by platformUiWar.
It calls the shared REST endpoint and keeps the access token only in the page's
password input. No ERP UI library, persistent token storage or duplicated service
implementation is used. This is a validation UI, not a production design system.
