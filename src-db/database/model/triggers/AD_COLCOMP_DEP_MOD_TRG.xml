<?xml version="1.0"?>
  <database name="TRIGGER AD_COLCOMP_DEP_MOD_TRG">
    <trigger name="AD_COLCOMP_DEP_MOD_TRG" table="AD_COLUMN_COMP_DEPENDENCY" fires="before" insert="true" update="true" delete="true" foreach="row">
      <body><![CDATA[

/*
 *************************************************************************
 * The contents of this file are subject to the Etendo License
 * (the "License"), you may not use this file except in compliance with
 * the License.
 * You may obtain a copy of the License at
 * https://github.com/etendosoftware/etendo_core/blob/main/legal/Etendo_license.txt
 * Software distributed under the License is distributed on an
 * "AS IS" basis, WITHOUT WARRANTY OF ANY KIND, either express or
 * implied. See the License for the specific language governing rights
 * and limitations under the License.
 * All portions are Copyright © 2021–2025 FUTIT SERVICES, S.L
 * All Rights Reserved.
 * Contributor(s): Futit Services S.L.
 *************************************************************************
 */
  devTemplate NUMBER;
  devModule   CHAR(1);
  cuerrentModuleID  VARCHAR2(32);
  vAux NUMBER;

BEGIN

    IF AD_isTriggerEnabled()='N' THEN RETURN;
    END IF;

  --Check if trying to move object from module not in dev
  IF (UPDATING) THEN
    IF (COALESCE(:NEW.AD_Module_ID , '.') != COALESCE(:OLD.AD_Module_ID , '.')) THEN
      SELECT COUNT(*)
        INTO vAux
        FROM AD_MODULE
       WHERE AD_MODULE_ID = :old.AD_Module_ID
        AND isindevelopment = 'N';
      IF (vAux!=0) THEN
        RAISE_APPLICATION_ERROR(-20000, '@ChangeNotInDevModule@');
      END IF;
    END IF;
  END IF;

  SELECT COUNT(*)
    INTO devTemplate
    FROM AD_MODULE
   WHERE IsInDevelopment = 'Y'
     AND Type = 'T';

  IF (UPDATING OR INSERTING) THEN
    cuerrentModuleID := :new.AD_Module_ID;
  ELSE
    cuerrentModuleID := :old.AD_Module_ID;
  END IF;

  SELECT M.IsInDevelopment
    INTO devModule
    FROM AD_MODULE M
   WHERE M.AD_MODULE_ID = cuerrentModuleID;

  IF (UPDATING AND devTemplate=0 AND devModule='N') THEN
    IF (
        COALESCE(:NEW.AD_Client_ID , '.') != COALESCE(:OLD.AD_Client_ID , '.') OR
        COALESCE(:NEW.AD_Org_ID , '.') != COALESCE(:OLD.AD_Org_ID , '.') OR
        COALESCE(:NEW.IsActive , '.') != COALESCE(:OLD.IsActive , '.') OR
        COALESCE(:NEW.AD_Column_ID , '.') != COALESCE(:OLD.AD_Column_ID , '.') OR
        COALESCE(:NEW.Source_Table_ID , '.') != COALESCE(:OLD.Source_Table_ID , '.') OR
        COALESCE(:NEW.Insert_Event , '.') != COALESCE(:OLD.Insert_Event , '.') OR
        COALESCE(:NEW.Update_Event , '.') != COALESCE(:OLD.Update_Event , '.') OR
        COALESCE(:NEW.Delete_Event , '.') != COALESCE(:OLD.Delete_Event , '.') OR
        COALESCE(:NEW.Target_ID_Resolver_SQL , '.') != COALESCE(:OLD.Target_ID_Resolver_SQL , '.') OR
        COALESCE(:NEW.Target_Link_Column_ID , '.') != COALESCE(:OLD.Target_Link_Column_ID , '.') OR
        COALESCE(:NEW.SeqNo, -1) != COALESCE(:OLD.SeqNo, -1)) THEN
      RAISE_APPLICATION_ERROR(-20000, '@20532@');
    END IF;
  END IF;

  IF ((DELETING OR INSERTING) AND devModule='N') THEN
    RAISE_APPLICATION_ERROR(-20000, '@20533@');
  END IF;
END AD_COLCOMP_DEP_MOD_TRG
]]></body>
    </trigger>
  </database>
